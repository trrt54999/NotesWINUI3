<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="practice2_OPAM_KN24_Daniel_Batko.Pages.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:practice2_OPAM_KN24_Daniel_Batko.Pages"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel
            Width="400"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="16">
            <TextBlock
                Margin="0,0,0,20"
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Registration" />

            <TextBox
                x:Name="UsernameTextBox"
                Header="Login"
                PlaceholderText="Enter login"
                Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock x:Name="UsernameErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnUsernameChanged(ViewModel.Username), Mode=OneWay}" />

            <TextBox
                x:Name="EmailTextBox"
                Header="Email"
                PlaceholderText="Enter email"
                Text="{x:Bind ViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock x:Name="EmailErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnEmailChanged(ViewModel.Email), Mode=OneWay}" />

            <TextBox
                x:Name="FirstNameTextBox"
                Header="First name"
                PlaceholderText="Enter first name"
                Text="{x:Bind ViewModel.FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock x:Name="FirstNameErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnFirstNameChanged(ViewModel.FirstName), Mode=OneWay}" />

            <TextBox
                x:Name="LastNameTextBox"
                Header="Last name"
                PlaceholderText="Enter last name"
                Text="{x:Bind ViewModel.LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock x:Name="LastNameErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnLastNameChanged(ViewModel.LastName), Mode=OneWay}" />

            <PasswordBox
                x:Name="InputPasswordBox"
                Header="Password"
                Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="Enter password" />
            <TextBlock x:Name="InputPasswordErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnPasswordChanged(ViewModel.Password), Mode=OneWay}" />

            <PasswordBox
                x:Name="ConfirmPasswordBox"
                Header="Confirm password"
                Password="{x:Bind ViewModel.ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="Confirm password" />
            <TextBlock x:Name="ConfirmPasswordErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnConfirmPasswordChanged(ViewModel.ConfirmPassword, ViewModel.Password), Mode=OneWay}" />

            <TextBlock
                x:Name="RegisterErrorTextBlock"
                Text="A user with this login already exists"
                Visibility="Collapsed" />

            <Button
                x:Name="RegisterButton"
                Margin="0,10,0,0"
                HorizontalAlignment="Stretch"
                Click="RegisterButton_Click"
                Content="Register" />

            <HyperlinkButton
                x:Name="LoginLink"
                HorizontalAlignment="Center"
                Click="LoginLink_Click"
                Content="Already have an account? Sign in" />
        </StackPanel>
    </Grid>
</Page>