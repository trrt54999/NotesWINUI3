<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="practice2_OPAM_KN24_Daniel_Batko.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:practice2_OPAM_KN24_Daniel_Batko.Pages"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel
        MaxWidth="400"
        Padding="32"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="16">
        <TextBlock
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Log in" />

        <TextBox
            x:Name="IdentifierTextBox"
            Header="Login or Email"
            PlaceholderText="Enter login or email"
            Text="{x:Bind ViewModel.Identifier, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock x:Name="IdentifierErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnIdentifierChanged(ViewModel.Identifier), Mode=OneWay}" />

        <PasswordBox
            x:Name="InputPasswordBox"
            Header="Password"
            Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            PlaceholderText="Enter password" />
        <TextBlock x:Name="InputPasswordErrorTextBlock" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="{x:Bind OnPasswordChanged(ViewModel.Password), Mode=OneWay}" />

        <TextBlock
            x:Name="LoginErrorTextBlock"
            Text="Incorrect login/email or password"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Visibility="Collapsed" />

        <Button
            x:Name="LoginButton"
            Margin="0,10,0,0"
            HorizontalAlignment="Stretch"
            Click="LoginButton_Click"
            Content="Enter" />

        <HyperlinkButton
            x:Name="RegisterLink"
            HorizontalAlignment="Center"
            Click="RegisterLink_Click"
            Content="Don't have an account? Sign up" />
    </StackPanel>
</Page>